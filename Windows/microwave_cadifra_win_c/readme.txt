Example realizing a microwave oven. More info is available in the manual.

To build this example on Windows make sure:
- Java Runtime Environment (JRE) is installed
- Visual Studio Build Tools are installed (CL.exe, nmake, ...)

To build the oven.exe call in a Developer Command Prompt Window

nmake clean
nmake
